#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "public.h"

// file.c
FILE * open_file(const char *, const char *, const char *, char **);

// string.c
unsigned char get_hex_digit(char);
